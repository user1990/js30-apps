{
  "name": "geolocation",
  "version": "1.0.0",
  "description": "geolocation",
  "main": "scripts.js",
  "scripts": {
    "start": "browser-sync start --directory --server --files '*.css, *.html, *.js' --https"
  },
  "author": "user1990",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.1"
  }
}
